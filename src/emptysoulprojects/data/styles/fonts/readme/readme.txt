used fonts:

font_1 : Roboto [bold,light,regular,thin]
font_2 : JosefinSans [bold,light,regular,thin]

---------------------------------------
_default: fonts for page defaults (loading,ie/errors ect...)
          "default"-fonts are used normally in the web-page too.

---------------------------------------

to change your fonts, just replace the original (font_1/font_2).

--------------------------------------

to change/rename your default font/fonts change the name of the files and the filenames in the "../_defaults/default_font.css" too.

-------------------------------------

fonts exported to import_default_styles -> imported into App.jsx
